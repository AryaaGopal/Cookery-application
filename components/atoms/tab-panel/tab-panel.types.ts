/**
 * Interface representing the props for the TabPanel component.
 */
export interface TabPanelProps {
    /** The content to be displayed within the TabPanel. */
    children?: React.ReactNode;
    /** Id of the TabPanel */
    id: string;
    /** Class of the TabPanel */
    className: string;
  }
  